"""
1. Return the ceiling of x, the smallest integer greater than or equal to x. If x is not a float, delegates to
x.__ceil__(), which should return an integral value

2. Return the absolute value of x

3. Return  factorial as an integer. Raise value error if x is not integral or visisable

4. Return the floor of x, the largest integer less than or equal to x. if x is not float, delegatess,
to x .__floor__, which should return an integral value

5. Return an accurate floating point sum of values in the iterable

6. Square Root

"""
import math

x = 100
# print(math.ceil(x))
#
# print(math.fabs(x))
#
# print(math.factorial((x)))
# print(math.floor(x))

# l = [12,13,14,14,15]
# print(math.fsum(l))

print(math.sqrt(x))