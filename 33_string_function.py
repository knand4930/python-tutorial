"""
1. chr() : this takes in a integer i and converts it to a characters c, so it returns a character string
#Convert integer 65 to ASC|| character('A')
#ASC value start with 65(A), 66(B)

2. ord() : this Takes a single unicode character(string of length) and returns an integer, so the format is:
#Convert ASC|| unicode Character 'A' to 65

"""


a = 90
print(chr(a))

a = 'a'
print(ord(a))